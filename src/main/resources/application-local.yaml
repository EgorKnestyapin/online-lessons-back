spring:
  datasource:
    url: jdbc:mysql://localhost:3306/online_lessons
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      max-request-size: 10GB

logging:
  file:
    name: logs/log.log
  level:
    root: info

server:
  address: localhost
  port: 8069
